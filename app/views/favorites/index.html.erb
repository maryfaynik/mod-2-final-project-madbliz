<h1>  Your Favorites! </h1>

<%= @favorites.empty? ? (link_to "Start making some madlibs you'll love!", new_madlib_path) : nil %>

<ul>
<% @favorites.each do |favorite|%>
    <% madlib = Madlib.find(favorite.madlib_id)%>
    <li> <%=link_to madlib.title, madlib_path(madlib) %> 
        <%= button_to "Un-Favorite", {action: "destroy", id: favorite.id}, method: :delete %>
    </li>
<%end%>
</ul>